using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class hasDescription : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Do Nothing
        //Script is only used as a reference point / marker so interactDescription can check if
        //the object has a description
    }

    // Update is called once per frame
    void Update()
    {
        //Do nothing
    }
}
